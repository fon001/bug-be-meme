apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pbmonitor.fullname" . }}-test-connection"
  labels:
    {{- include "pbmonitor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pbmonitor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
