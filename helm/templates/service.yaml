apiVersion: v1
kind: Service
metadata:
  name: {{ include "pbmonitor.fullname" . }}
  labels:
    {{- include "pbmonitor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pbmonitor.selectorLabels" . | nindent 4 }}
